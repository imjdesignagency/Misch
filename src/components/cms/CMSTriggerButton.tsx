import React from 'react';
import { Settings, Sparkles, Eye, EyeOff } from 'lucide-react';
import { motion } from 'motion/react';
import { useCMS } from '../../context/CMSContext';

export const CMSTriggerButton: React.FC = () => {
  return null;
};
